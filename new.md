Hi all. I've been learning Rust recently, and decided to build a minimal UNIX
shell as a learning project. I'm atttempting to implement a *very* stripped
down version of shell grammar that recognizes logical connectives - `&&` and
`||` (perhaps in the future it will implement the full POSIX grammar).

On to my parsers. I
